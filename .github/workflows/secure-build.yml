name: Secure Build Workflow (Disabled)

# This workflow is intentionally disabled
# For dotnet projects, the secure-build action is not compatible
# Use SonarQube console tool with proper dotnet configuration instead
# Contact DevOps team or .NET developers for proper setup

on:
  workflow_dispatch:  # Only allow manual triggering

jobs:
  placeholder:
    runs-on: ubuntu-latest
    if: false  # This ensures the job never runs
    steps:
      - name: Placeholder step
        run: echo "This workflow is disabled"

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     types: [ opened, synchronize, reopened ]

# jobs:
  # secure-build:
    # The stable version specified here is intentional.
    # Please consult with the Security team before making any changes.
    # uses: pdffiller/security_checks/.github/workflows/secure-build.yml@stable
    # secrets: inherit
